# killstreaks.yml

Killstreak rewards.

**How it plays:** [:octicons-arrow-right-24: read the guide](../../gameplay/kits.md#killstreaks)

## What matters

- **The reward table ships empty.**
- A reward fires at exactly its streak; `%player%` and `%streak%` are filled in.

Changes apply with `/hcf reload`.

## The shipped file

This is `plugins/HCFCore/killstreaks.yml` as the plugin writes it on the first start. Every comment is part of the reference. [View it on GitHub](https://github.com/Lawkeys-dev/hcf-core/blob/main/src/main/resources/killstreaks.yml).

<div class="hcf-shipped" markdown>

```yaml title="killstreaks.yml"
--8<-- "src/main/resources/killstreaks.yml"
```

</div>
