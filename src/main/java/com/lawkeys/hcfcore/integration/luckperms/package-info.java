/**
 * LuckPerms integration: reads player prefix/suffix for the chat format feature. Soft-dependency only, falls back to a plain format if LuckPerms is absent. See ARCHITECTURE.md section 11.
 */
package com.lawkeys.hcfcore.integration.luckperms;
