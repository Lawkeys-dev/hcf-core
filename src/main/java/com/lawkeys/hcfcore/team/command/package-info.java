/**
 * The team module's command layer.
 *
 * <p>These classes validate input, resolve players and worlds, and render the
 * outcome through the language file. They hold no rules of their own - every
 * decision is delegated to {@code TeamManager} (CONTRIBUTING.md section 3: "no
 * game logic in Command or Listener classes").
 */
package com.lawkeys.hcfcore.team.command;
